#include<stdio.h>
void main()
{
	char str[20];
	int i,count=0;
	printf("enter the string:\n");
	gets(str);
	for(i=0;str[i]!='\0';i++)
	{
		if(str[i]==' ')
		{
			count++;
		}
	}
	printf("words = %d",count+1);
}
