#include<stdio.h>
int main()
{
	int n;
	int a[100],i,sum=0,x;
	printf("enter n value :");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
		sum=sum+a[i];
	}
	n++;
	x=(n*(n+1))/2;
	printf("Missing number is %d",x-sum);
	return 0;
}
